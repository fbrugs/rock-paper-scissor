# Odin project: rock paper scissor

This is a simple rock, paper, scissor game playable
in the console located in the DevTools.

Computer choice is generated by using the Math.random() function
Human choice is selected by giving the user a prompt
